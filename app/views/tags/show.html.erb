<h1>Articles Tagged with <%= @tag.name %>  </h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>

<%= link_to "Delete This Tag", tag_path(@tag), method: :delete, data: {confirm: "Are you really REALY sure that you want to delete this tag?...it will be gone for like, forever"} %>
